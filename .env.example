VITE_POCKET_BASE="http://127.0.0.1:8090" # default port for pocketbase

# Playwright Envs
BASE_URL="http://localhost:5173" # default port for app
API_URL=""
USER_EMAIL=""
USER_PASSWORD=""

# Superuser creds used only by the e2e cleanup teardown to delete the users
# created by the signup tests. Optional: if unset, user cleanup is skipped
# (form cleanup still runs via USER_EMAIL/USER_PASSWORD).
PB_SUPERUSER_EMAIL=""
PB_SUPERUSER_PASSWORD=""